#include<stdio.h>
int main() {
int i,n,mul=1;
printf("Enter the value of n: ");
scanf("%d", &n);
for(i=1;i<=n;i++)
{
    if(i%2==0)
    {
        mul=mul*i;
    }
}
printf("Product of even numbers is %d\n", mul);
return 0;
}
